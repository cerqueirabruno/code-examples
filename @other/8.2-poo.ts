/*

CLASSES ABSTRATAS:
é uma classe que não pode ser instanciada diretamente.
Em vez disso, ela é projetada para ser uma classe base para outras classes.
Classes abstratas frequentemente incluem métodos que não possuem implementação (métodos abstratos)
e que devem ser implementados pelas subclasses. As classes abstratas são úteis para definir 
uma interface comum para um grupo de subclasses, garantindo que todas sigam a mesma estrutura
ou contrato.

COMPOSIÇÃO:

AGREGAÇÃO:

*/