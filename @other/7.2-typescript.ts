/*

=====> GENERICS:
permitem que você defina uma "caixa" de tipo que pode ser substituída por um tipo específico quando a
função, classe ou interface é usada.

=====> TYPES ASSERTIONS
forçar o TypeScript a considerar um valor como determinado tipo e desconsiderar eventuais erros gerados
em tempo de compilação.

*/