// Símbolos são únicos e imutáveis, o que significa que cada Symbol é diferente de qualquer outro Symbol, 
//  tornando-os ideais para criar propriedades de objeto privadas ou chaves únicas em coleções de dados.

const mySymbol = Symbol();

// {...}

console.log(mySymbol);