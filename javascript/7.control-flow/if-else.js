/*

IF, ELSE, ELSE IF

*/

if (condicao) {
	// código a ser executado, se a condição for verdadeira
} else if (condicao2) {
	// código a ser executado, caso a primeira condição não seja verdadeira
} else if (condicao3) {
	// código a ser executado, caso a segunda condição não seja verdadeira
} else {
	// código padrão, caso nenhuma condição atenda
}