/* TAMANHO */
* {
    /* largura */
    width: 100px;

    /* altura */
    height: 100px;
}

/* ESTRUTURA */
* {
    /* preenchimento */
    padding: 100px; /* todos os lados */
    padding: 100px 200px 300px 400px; /* sup, dir, inf, esq */

    /* bordas */
    border: 2px solid #000000; /* espessura, estilo, cor */
    border-radius: 2px; /* arredondar a borda */

    /* margens */
    margin: 100px;
    margin: 100px 200px 300px 400px; /* sup, dir, inf, esq */
}

/* DISPLAY */
* {
    /* display linha */
    display: inline;

    /* display coluna */
    display: block;

    /* display linha/colunas */
    display: inline-block;
}

/* COR DE FUNDO */
* {
    /* cor de fundo */
    background-color: #000000; /* cor */
    background-image: url(/images/bg.jpg); /* imagem de fundo */
    background-image: linear-gradient(red, blue); /* gradiente */
    background-image: linear-gradient(45deg, red, blue); /* gradiente */
    background-repeat: no-repeat; /* não repetir a imagem */
}

/* DESCEDÊNCIA DE SELETORES */
p div {
    font-size: 16px;
}

/* AGRUPAMENTO DE SELETORES */
p,
div {
    font-size: 16px;
}

/* TEXTO */
* {
    font-family: Arial; /* fonte */
    font-size: 20px; /* tamanho da fonte (padrão é 16px) */
    
    font-weight: bold; /* negrito */
    font-weight: 600; /* peso da fonte entre 100 a 900 */
    
    font-style: italic; /* itálico */
    font-style: oblique; /* oblíquo */
    
    text-transform: uppercase; /* maiúscula */
    text-transform: lowercase; /* minúscula */
    
    line-height: 10px; /* altura da linha */
    
    text-align: center; /* centro */
    text-align: right; /* direita */
    
    text-decoration: none; /* remove decoração (sublinhado/underline) */
    text-decoration: underline; /* adiciona decoração (sublinhado/underline) */
    
    text-shadow: 4px 5px 10px #000000; /* sombras (horizontal - vertical - desfoque - cor) */
    
    list-style-type: none; /* remove o disco da lista */
    list-style-type: decimal; /* adiciona lista numerada */
}